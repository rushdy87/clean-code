// Function to save a user data to a data base

// bad names
function process(user) {}
function handle(user) {}

// Okay name
function save(user) {}
function storeData(user) {}

// Good names
function saveUser(user) {}
user.store(); //store is a method in User class

// Function validate the user inpute
// Bad name
function process(user) {} //unspecific
function save(user) {} //,misleading

// Okay name
function validateSave(user) {}
function check(user) {}

// Good names
function validate(user) {}
function isValid(user) {}
