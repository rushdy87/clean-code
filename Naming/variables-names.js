// A user object
// Bad names
const u = { name: '', email: '', age: 0 };
const data = { name: '', email: '', age: 0 };

// Okay names
const userData = { name: '', email: '', age: 0 };
const person = { name: '', email: '', age: 0 };

// Good names
const user = { name: '', email: '', age: 0 };
const customer = { name: '', email: '', age: 0 };

// A user Input validation
// Bad name
let v = true;
let val = true;

// Okay name
let correct = true;
let validatedInput = true;

// Good name
let isValid = true;
let isCorrect = true;
